{
  "name": "platform-runner",
  "version": "1.0.0",
  "description": "Platform runner game for Farcaster",
  "scripts": {
    "build": "echo 'Building static site' && mkdir -p public && cp index.html manifest.json public/ || exit 0"
  },
  "static": "public"
}
